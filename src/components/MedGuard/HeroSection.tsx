const HeroSection = () => {
  return (
    <section className="text-center px-6 py-8 animate-fade-in">
      <h1 className="font-display text-display-lg text-foreground max-w-3xl mx-auto text-balance">
        Check if your medicine is safe for you — right now.
      </h1>
      <p className="mt-4 text-body-lg text-muted-foreground max-w-xl mx-auto">
        Scan a medicine strip and get a clear safety warning based on your situation.
      </p>
    </section>
  );
};

export default HeroSection;
